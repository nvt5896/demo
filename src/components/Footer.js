// Create a new file in the `demo\src\components` folder called `Footer.js` and add the following code:
// // Path: demo\src\components\Footer.js
import React from 'react';
function Footer() {
    return (
        <div>
            <h1>Footer</h1>
        </div>
    );

}
export default Footer;
// // // Now, when we run the application, we should see the Footer component at the bottom of the page.
// // // We can also add the Footer component to the About.js file, and any other page we create. This will allow us to display the footer on any page in our application.
// // // We can also add the Footer component to the App.js file, and it will be displayed on every page. This is a good way to add a footer to our application.
// // // We can also add the Footer component to the index.js file, and it will be displayed on every page. This is a good way to add a footer to our application.
// // // Create a new file in the `demo\src\components` folder called `Header.js` and add the following code:
// // // Path: demo\src\components\Header.js
// // import React from 'react';
// // function Header() {
// //     return (
// //         <div>
// //             <h1>Header</h1>
// //         </div>
// //     );
// // }
// // export default Header;
// // // Now, when we run the application, we should see the Header component at the top of the page.
// // // We can also add the Header component to the About.js file, and any other page we create. This will allow us to display the header on any page in our application.

